package com.albums.store

class OkHttpAdapterTests {

  /*  data class AlbumTest(val id : String, override var identifier: Int) : IModel

    @Test
    fun `test a OkHttpAdapter when network service gets a success it should send a model with success`() {
        val network = OkHttpAdapter()
        val request = HttpRequest("https://static.leboncoin.fr/img/shared/technical-test.json")
        val model = AlbumTest("", 0)
        val mockSuccessCompletion : (IModel) -> Unit = mock()
        network.request(request, model, mockSuccessCompletion){}

        verify(mockSuccessCompletion.invoke(any()))
    }

    @Test
    fun `test a OkHttpAdapter when network service gets a code between 200 and 299 it should send a response with success`() {
        val network = OkHttpAdapter()
        val request = HttpRequest("https://static.leboncoin.fr/img/shared/technical-test.json")
        val model = AlbumTest("", 0)
        val mockSuccessCompletion : (IModel) -> Unit = mock()
        network.request(request, model, mockSuccessCompletion, any())


    }

    @Test
    fun `test a OkHttpAdapter when network service gets a code not between 200 and 299 it should send an failure with error`() {
    }

    @Test
    fun `test a OkHttpAdapter work when network service fails to get resources it should send failure with error`() {
    }*/
}

