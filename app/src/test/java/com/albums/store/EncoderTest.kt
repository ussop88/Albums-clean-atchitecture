package com.albums.store

import org.junit.Test

class EncoderTest {

    @Test
    fun `test Encoder and there is a valid data when it encodes it should notify success`() {
    }

    @Test
    fun `test Encoder and there is invalid data when it encodes it should notify failure`() {
    }
}