package com.albums.store

import org.junit.Test


class StoreTest {

    @Test
    fun `test a store that contains available resources when user asks for model's resources it should get resources`() {
    }

    @Test
    fun `test a store without available resources when user asks for resources it should get failure`() {
    }

    @Test
    fun `test a store not registered to adapter when user asks for registration it should register to it`() {
    }

    @Test
    fun `test a store when user asks for registration to adapter it should register to it`() {
    }

    @Test
    fun `test a store when user asks for model's resources it should get model's adapter`() {
    }

}