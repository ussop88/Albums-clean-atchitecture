package com.albums.cleanarchitecture.store

object Configuration {
    enum class URL_PARAMETERS(var value: String) {
        HOSTNAME("https://static.leboncoin.fr"),
        PATH("img/shared/technical-test.json"),
    }
}
